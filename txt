Tell Codex this is now clearly a broken validation mapping issue between:

* department assignments
* generated checkpoints
* department ownership
* validation resolver

The UI visibly shows assigned team members, but validation still thinks departments are unassigned.

That means the validator is checking the wrong data structure or stale state.

Use the screenshot as proof.

---

# CURRENT BUG

Error shown:

```text
A selected department has checkpoints assigned but no team member selected.
```

But:

* Safety/PSM has member
* PM Operation has member
* Process has member

So validation is failing incorrectly.

---

# ROOT CAUSE

Most likely one of these:

---

## ISSUE 1 — VALIDATION CHECKING STATIC MASTER DEPARTMENTS

Current validator probably loops through ALL departments:

```ts
ALL_DEPARTMENTS.forEach(...)
```

instead of ONLY departments that actually own generated checkpoints.

Example:

* Fire
* Civil
* Electrical

may have no checkpoints assigned,
but validator still expects members.

Wrong.

---

# REQUIRED FIX

Validation must ONLY check departments that actually have generated checkpoints.

Example:

```ts
const activeDepartments = generatedCheckpoints
  .map(c => c.department)
  .filter(unique)
```

Then validate ONLY those departments.

---

# CORRECT VALIDATION RULE

If:

```text
department owns checkpoints
```

THEN:

```text
department must have assigned member
```

Otherwise:
DO NOT validate it.

---

# ISSUE 2 — STALE ASSIGNMENT STATE

Possible current behavior:

UI card updates visually,
BUT validation reads old state object.

Example:

```ts
teamAssignmentsUI
```

vs

```ts
workflowAssignments
```

One updates.
Other does not.

---

# REQUIRED FIX

Need SINGLE assignment state.

---

# REQUIRED IMPLEMENTATION

Create canonical source:

```ts
const assignedDepartments = assignments.filter(
  assignment => assignment.memberId
)
```

Validator must read THIS ONLY.

NOT:

* rendered UI rows
* local component state
* placeholder department arrays

---

# REQUIRED DEBUGGING TARGETS

Codex must inspect:

```text
CreatePSSR.tsx
validation.ts
workflowValidation.ts
assignmentReducer.ts
checkpointMapping.ts
annexure generation logic
```

Find:

* where generated checkpoint departments are stored
* where assigned members stored
* why validator mismatch occurs

---

# IMPORTANT EDGE CASE

Departments with:

```text
0 checkpoints
```

must NEVER require member assignment.

Example:

* Fire department row visible
* but no Fire checkpoints selected

Then:

```text
Fire assignment optional
```

NOT mandatory.

---

# REQUIRED VALIDATION FLOW

Correct refinery workflow:

```text
Selected Annexures
→ Generate Checkpoints
→ Determine Active Departments
→ Require Assignments ONLY for Active Departments
→ Validation passes
```

---

# REQUIRED UI IMPROVEMENT

Rows without required assignments should visually show:

```text
No checkpoints assigned
```

instead of silently failing validation.

Example:

| Department | Status                  |
| ---------- | ----------------------- |
| Fire       | No checkpoints assigned |
| Electrical | No checkpoints assigned |
| Safety     | Assignment required     |

---

# REQUIRED SUBMIT LOGIC

Submit button should validate:

```ts
activeDepartments.every(dept =>
  assignments.some(a =>
    a.department === dept &&
    a.memberId
  )
)
```

NOT:

* every visible department row
* every master department
* every static table entry

---

# IMPORTANT

This is NOT a UI issue anymore.

This is:

* workflow-state architecture issue
* validation source mismatch
* refinery mapping logic issue

Codex must stop patching symptoms and fix the underlying state model properly.

Right now:

* annexures
* checkpoints
* assignments
* validation

are still partially disconnected.

They need to be derived from one canonical workflow state tree.
