Use something like this for Codex. Be very explicit that it must inspect the existing implementation first and not blindly patch files.

---

# TASK: Audit and Fix PSSR Assignment, Attachments, and Punch Point Workflow

Before making any changes, perform a complete codebase review.

Inspect:

* Backend models
* SQLAlchemy entities
* Alembic migrations
* PSSR workflow service
* Team service
* Punch point service
* Assignment logic
* Checkpoint response logic
* Attachment endpoints
* Frontend React pages
* Dashboard pages
* PSSR detail modal
* Assigned Punch Points tab
* History tab
* API service layer
* Type definitions

Do NOT assume behavior.

Trace actual execution flow from:

PSSR Creation → Department Assignment → Team Member Response → Initiator Review → Area Owner Routing → Area Owner Approval → Punch Point Assignment → Punch Point Closure

Document findings before implementing changes.

---

# ISSUE 1 — INCORRECT "ASSIGNED BY" USER

Current behavior:

In Assigned Punch Points the UI displays:

```text
Assigned By: Chad Garcia
Assigned By: Danielle Richardson
Assigned By: Ronald Mathis
```

These users are team members.

This is incorrect.

Business rule:

ONLY these roles may assign punch points:

* Initiator
* Area Owner

Team members must NEVER appear as the assigner.

Required fix:

Determine where the assigner is currently being populated.

Audit:

* punch point creation logic
* punch point reassignment logic
* workflow routing logic
* database records

Ensure:

```text
assigned_by_user_id
```

always stores:

```text
Initiator User ID
OR
Area Owner User ID
```

Never the assigned team member.

UI must display:

```text
Assigned By:
<Name>
<Employee ID>
<Role>
```

based on actual assigner.

---

# ISSUE 2 — ATTACHMENTS MUST BE OPTIONAL

Current requirement:

Checkpoint response can contain:

* Yes
* No
* N/A

with or without attachment.

Attachment is NOT mandatory.

Required behavior:

Valid:

```text
YES
Remarks only
```

Valid:

```text
YES
Attachment only
```

Valid:

```text
YES
Remarks + Attachment
```

Valid:

```text
YES
Nothing else
```

Same for:

```text
NO
N/A
```

System must never force attachment upload.

Audit:

* frontend validation
* backend validation
* response schemas

Remove all attachment-required validation.

---

# ISSUE 3 — VIEW BUTTON SHOULD NOT DOWNLOAD FILE

Current behavior:

When user clicks:

```text
View
```

file downloads.

This is wrong.

Required behavior:

Images:

```text
jpg
jpeg
png
```

must open in browser preview.

PDF:

```text
pdf
```

must open in browser tab.

Only Download button should download.

Required implementation:

Backend:

Provide:

```http
Content-Disposition: inline
```

for preview endpoint.

Create separate endpoints:

```http
GET /attachments/{id}/view
GET /attachments/{id}/download
```

View:

```text
inline
```

Download:

```text
attachment
```

Frontend:

Buttons:

```text
View
Download
```

must use separate endpoints.

---

# ISSUE 4 — ATTACHMENT VISIBILITY

All uploaded files must be visible to:

* Team Members
* Department Leaders
* Initiator
* Area Owner

Visibility must remain throughout workflow.

Audit:

* authorization layer
* attachment queries
* checkpoint detail responses

Ensure attachment metadata includes:

```text
File Name
Uploaded By
Employee Code
Department
Uploaded At
File Type
```

---

# ISSUE 5 — RESPONSE AUDIT TRAIL

Every checkpoint answer must permanently show:

```text
Answered By:
Name

Employee ID

Department

Role

Timestamp
```

for:

```text
YES
NO
N/A
```

responses.

Current implementation may be incomplete.

Audit all response DTOs and API payloads.

Ensure history survives page refresh and workflow transitions.

---

# ISSUE 6 — TEAM MEMBERS MAY EDIT UNTIL AREA OWNER APPROVAL

Business rule:

Team members can continue editing checkpoint responses until Area Owner approves the PSSR.

Allowed states:

```text
DRAFT
IN_PROGRESS
DEPARTMENT_COMPLETED
WAITING_FOR_AREA_OWNER
```

Editing should remain enabled.

Lock responses ONLY when:

```text
AREA_OWNER_APPROVED
```

or

```text
CLOSED
```

Audit:

* frontend disabling logic
* backend permission checks

Ensure backend enforces the rule.

---

# ISSUE 7 — ASSIGNED PUNCH POINT TAB AUDIT

Audit the newly added:

```text
Assigned Punch Points
```

tab.

Verify:

* only assigned punch points appear
* assignments come from database
* no duplicate entries
* proper pagination
* role-based visibility

Display:

```text
Punch Point Title
PSSR ID
Assigned By
Assigned To
Department
Priority
Category
Due Date
Status
Created Date
```

---

# ISSUE 8 — SEARCH FUNCTION FOR MEMBER ASSIGNMENT

Current search is unreliable.

Required behavior:

Search must support:

```text
Name
Employee ID
Department
Email
```

Partial matches.

Examples:

```text
john
```

returns:

```text
John Smith
John Doe
```

Example:

```text
1002
```

returns:

```text
NYR-SAF-1002
```

Example:

```text
safety
```

returns Safety department users.

Search should be:

```text
case-insensitive
debounced
server-side
```

Audit backend query performance.

---

# ISSUE 9 — FULL CODE REVIEW

Before changing anything:

Trace:

* PSSR workflow states
* Punch point lifecycle
* Checkpoint lifecycle
* Assignment lifecycle
* Attachment lifecycle

Verify no regressions.

After implementation provide:

1. Files modified
2. Database changes
3. API changes
4. Workflow changes
5. Frontend changes
6. Migration requirements
7. Testing checklist
8. Manual verification steps

Do not provide partial fixes.

Provide a complete implementation that preserves existing data and workflow behavior.
